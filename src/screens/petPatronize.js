import React from 'react';
import { ScrollView } from 'react-native';
import {
    Text
} from 'react-native-paper';
import { Container } from '../components/layout';


export default function PetPatronize() {
    return (
        <ScrollView>
            <Container>
                <Text>Pet Patronize</Text>
            </Container>
        </ScrollView>
    );
}
